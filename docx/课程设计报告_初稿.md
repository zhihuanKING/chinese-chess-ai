# 有限算力预算下的中国象棋学习型博弈 AI
## ——基于监督冷启动 + Gumbel-AlphaZero 的样本高效自对弈与传统搜索对照研究

> 课程设计报告 · 初稿(实验已收尾,§7 全部图表均由真实日志/checkpoint 重算生成,无占位待填项)
> 撰写语言:中文

---

## 摘要

本文在 **8×A800(40GB)单机、约一周训练预算**的强约束下,设计并实现了一个中国象棋学习型博弈 AI。核心方法为**两阶段管线**:先用 Pikafish 标注数据做**监督冷启动**(256×15 ResNet),再以 **DAgger 分布修复蒸馏 + AlphaZero/Gumbel-AlphaZero 范式 + 向量化批量 MCTS** 提升棋力,并以传统 **Alpha-Beta 引擎**及 **Pikafish 限档**作外部锚点。系统采用 **C++ 规则内核(pybind11 暴露)+ Python 训练/搜索**的混合架构,规则内核以 perft 对拍达到逐位正确(perft(1..5)=44/1920/79666/3290240/133312995)。**实验结果**(128 局/锚点、修复版评测协议、外部锚点 Elo):监督模型学会下真实象棋(对随机走子 100% 胜,top-1 模仿 96.7%);最终模型 r1 对 6 个引擎锚点**平均 +203 Elo**(对自研 Alpha-Beta 约 28%、Pikafish d1 约 20%),把差距收窄约 200 Elo,但**所有预算下仍弱于 Alpha-Beta,未出现 crossover 反超**。增益**全部来自监督蒸馏 + DAgger 分布修复**;自对弈 RL 在管线 bug 修复后机制正确、在弱底座上可爬升,但在已较强的底座上对最终棋力**零贡献**。本文的核心贡献是:**实证刻画了有限算力下学习型方法相对传统搜索的能力边界,定位了真正起作用的杠杆(分布修复)与失效方向(强底座低模拟自对弈、标签工程),并沉淀了一套以外部锚点为准、防 net-vs-net Elo 通胀的评测方法论。**

**关键词**:中国象棋;AlphaZero;Gumbel-AlphaZero;蒙特卡洛树搜索;监督冷启动;Alpha-Beta 搜索

---

## 第1章 绪论

### 1.1 研究背景
博弈一直是人工智能的试金石。从深蓝(Deep Blue)以 Alpha-Beta 搜索+人工评估战胜国际象棋世界冠军,到 AlphaGo(2016)、AlphaGo Zero(2017)、AlphaZero(2018)以"深度神经网络 + 蒙特卡洛树搜索(MCTS)+ 自对弈强化学习"统一了完全信息博弈的求解范式,博弈 AI 经历了从"人写规则"到"自我学习"的代际跨越。2022 年 Gumbel-AlphaZero 进一步在低模拟预算下提供了带理论保证的策略改进,2024–2025 年又涌现 ReZero、EfficientZero V2、Search-contempt 等面向有限算力的改进。

### 1.2 中国象棋的博弈学定性
中国象棋是**完全信息、确定性、双人零和、回合制**博弈:无隐藏信息、无机会节点。这一定性决定了算法选型——它属于 AlphaZero/Alpha-Beta 擅长的领域,而不适用于面向不完全信息的 CFR 系方法(见第 2 章)。

### 1.3 研究问题与创新点
**研究问题**:在仅 8×A800、约一周训练预算的强约束下,"监督冷启动 + 低模拟自对弈"两阶段管线能否训练出显著超越同等时间预算 Alpha-Beta 引擎的学习型 AI?

**创新点**:
1. 以 **Gumbel-AlphaZero(ICLR 2022)** 作为核心训练算法,低模拟数仍保证策略改进;
2. **Pikafish 监督冷启动 + DAgger 分布修复蒸馏**两阶段管线,破解一周算力约束(并规避了人类棋谱下载受限的工程问题);实证表明分布修复(把训练局面对齐到模型实战分布)是有限算力下最有效的棋力杠杆;
3. **C++ 规则内核 + Python 向量化批量 MCTS** 的混合架构,以集群级聚合利用率吃满 8 卡;
4. 针对象棋的定制:from→to 动作编码 + 合法掩码 + 红黑对称视角规范化。

### 1.4 报告组织
第 2 章相关技术调研;第 3 章环境建模与正确性验证;第 4 章算法设计;第 5 章工程实现与系统架构;第 6 章实验设置;第 7 章实验结果与分析;第 8 章总结与展望;附录含超参、复现说明、参考文献与开源声明。

---

## 第2章 相关技术(调研)

按"完全信息 vs 不完全信息"分水岭组织,覆盖任务书点名的四类技术。

### 2.1 Minimax 与 Alpha-Beta 剪枝
经典对抗搜索。本文基线引擎实现了 negamax + α-β 剪枝 + 置换表(Zobrist)+ 迭代加深 + 静态搜索(quiescence)+ MVV-LVA 走子排序,配分阶段手写评估。

### 2.2 MCTS / PUCT
蒙特卡洛树搜索以"选择-扩展-评估-回传"四步增量构建搜索树;AlphaZero 采用 PUCT 选择 `a*=argmax[Q+c_puct·P·√ΣN/(1+N)]`,以神经网络先验 P 引导搜索。

### 2.3 AlphaGo Zero / AlphaZero
策略-价值双头网络 + MCTS 自我对弈,无需人类知识即可从零超越顶级程序;在将棋(与象棋同构)上 <24h 达到超人水平。

### 2.4 Gumbel-AlphaZero(核心)
Danihelka et al., ICLR 2022。根节点以 Gumbel-Top-k + Sequential Halving 在少量模拟内集中预算,非根节点用 completed-Q 确定性选择,**在低模拟数(16~32)下仍有策略改进的理论保证**,样本效率较原版高约一个数量级。这是本文"有限算力破局"的算法答案。

### 2.5 MuZero(辨析:不实现)
Schrittwieser et al., 2020。价值在于环境模型未知时联合学习 dynamics;中国象棋规则完全已知,学 dynamics 徒增三网联合与调参复杂度且无精度优势,故仅调研、不作主线。

### 2.6 不完全信息博弈方法(辨析:不适用)
DeepCFR / DeepStack / Libratus / Pluribus / Suphx 针对扑克、麻将等**不完全信息**博弈,在 information set 上求纳什均衡;象棋无隐藏信息,CFR 框架无作用对象。Student of Games(Science 2023)、AlphaZe**(2023)则尝试统一两类博弈,可作前沿展望。

### 2.7 前沿动态(2024–2026)
ReZero(reanalyze 加速)、EfficientZero V2、UniZero、Search-contempt(有限算力训练)、Searchless Chess / Chessformer / Maia(Transformer 路线)。本文以 Gumbel-AZ 为核心并借鉴 KataGo 的工程省样本技巧(playout cap randomization 等)。

> 表2.1 各算法适用博弈类型与本文取舍 [可整理成表]

---

## 第3章 环境建模与正确性验证

### 3.1 棋盘与走法
9 列 × 10 行 = 90 格,mailbox(int8[90])表示。实现完整走法:将/帅(九宫内、飞将判定)、士(九宫斜走)、象(田字、塞象眼、不过河)、马(日字、蹩马腿)、车(直线滑动)、炮(隔且仅隔一子吃)、兵卒(过河前只进、过河后可平不可退)。合法着法过滤送将/自杀着。

### 3.2 规则与终局
将军/应将、将死、困毙(无合法着且未被将判负)、第 3 次重复判和、120 回合(240 半回合)自然限着判和。长将/长捉本版按重复处理(简化版,报告声明,完整规则列展望)。

### 3.3 表示与编码
Zobrist 增量哈希(含行动方位,make/unmake 对称)、FEN 往返无损;动作编码 **move = from×90 + to ∈ [0,8100)**,配合合法掩码(2086 紧凑编码列为后续消融);输入特征 C×10×9(C=15:己方7+对方7+重复计数1),**以当前行动方视角规范化**(黑走时上下翻转+红黑互换)。

### 3.4 正确性验证(最高优先级)
- **perft 对拍**:从初始局面 perft(1..5) = **44 / 1920 / 79666 / 3290240 / 133312995**,与公开参考值逐位一致。
- **规则单元测试**:飞将、蹩马腿、塞象眼、炮隔子吃、送将过滤等共 31 项断言全部通过。
- **一致性**:Zobrist make/unmake 对称(200 步压力测试增量哈希=重算)、FEN 往返无损、输入编码↔解码视角一致(红黑对称专项测试通过)。

> 表3.1 perft 对拍结果(d=1..5,本实现 vs 参考,逐位一致)
> 表3.2 规则单元测试覆盖与结果

---

## 第4章 算法设计

### 4.1 策略-价值网络
ResNet 主干:Conv3×3(C)+BN+ReLU → N 个残差块 → 双头。Policy 头:Conv1×1→Flatten→Linear→8100 logits(+合法掩码,非法置 -inf 后 softmax);Value 头:Conv1×1→Flatten→Linear→Linear(1)→tanh∈[-1,1]。本文用 **128 通道×10 块(≈26.4M 参数)**作主力,256×15(≈41.3M)作规模消融。

### 4.2 损失函数
`L = (z−v)² − πᵀlog p + c‖θ‖²`,c=1e-4(L2 由 AdamW weight_decay 实现)。Gumbel 版 π 用 completed-Q 改进策略作目标。

### 4.3 搜索:PUCT 与 Gumbel-MCTS
统一 `Planner.search(positions, net, n_sim)` 接口,**向量化批量**实现:同时处理 B 局并行对局,把各局待评估叶子拼成一个大 batch 一次喂 GPU(吃满 GPU 的关键)。PUCTPlanner(根节点 Dirichlet 噪声、虚拟损失)先行跑通学习曲线;GumbelPlanner(Sequential Halving + completed-Q)作核心创新对照。

### 4.4 两阶段训练管线
1. **监督冷启动**:用 Pikafish 自对弈生成的 (局面, 着法, 结果) 监督训练 policy+value,快速跳过随机乱走早期;
2. **自对弈 RL**:actor-learner 架构,自对弈样本入共享 ReplayBuffer,learner 采样训练并周期导出权重,worker 热加载;Elo-gating 晋升最强权重。

### 4.5 Alpha-Beta 基线
见 2.1;接 UCI 子集便于 Arena 对弈与同时间预算对照。

![图4.1 策略-价值双头残差网络结构](../logs/fig/fig_arch_network.png)
**图4.1** 策略-价值双头网络(PVNet):Input(15×10×9)→Conv3×3 主干→N 个残差块→分叉双头(Policy 8100 logits+合法掩码 / Value tanh∈[-1,1]);主力 128×10(≈26.4M)、最终 256×15(≈41.3M)。

![图4.2 actor-learner 自对弈训练闭环](../logs/fig/fig_arch_actor_learner.png)
**图4.2** actor-learner 闭环:GPU1–7 自对弈 actor(每卡多 worker,向量化批量 MCTS)产样本→/dev/shm 共享 ReplayBuffer→GPU0 learner(AdamW/BF16)训练并周期导出 latest.pt→actor 轮询 mtime 热加载;旁路 Elo-gating 晋升最强权重。

---

## 第5章 工程实现与系统架构

### 5.1 混合架构与语言边界
- **C++17**:规则内核(perft 硬验证)+ Alpha-Beta 引擎,经 **pybind11** 暴露为 `xqai._xqcore`,经 scikit-build-core 随包编译;
- **Python + PyTorch 2.6(cu124)**:网络、向量化 MCTS、自对弈、训练、评估;
- 唯一跨语言契约 = checkpoint 权重文件,进程级解耦=风险隔离。

### 5.2 向量化批量 MCTS(吞吐杠杆)
单进程维护 B 局并行对局,叶子拼 batch 一次推理(参考 TurboZero/MCTX)。**权衡说明**:相比 C++ 多进程集中推理服务,本文选择 Python 向量化 + 多 worker 进程,以最小化系统复杂度换取一天约束下的迭代速度与正确性确定性,用"每卡多 worker + 集群聚合利用率"补回单任务效率。

### 5.3 分布式拓扑(独享 8×A800)
GPU0 learner + GPU1–7 自对弈(每卡多 worker);ReplayBuffer 驻 /dev/shm(多进程共享内存,容量 200 万局面);数据/权重落 NVMe。worker 轮询 latest.pt 的 mtime 热加载。

### 5.4 工程实现中的关键问题与解决(体现工程深度)
- **冷启动加载**:learner 按 checkpoint 内记录的 channels/blocks 建网,加载失败即显式中止,杜绝"静默退化为从零训练"浪费算力;
- **自对弈喂数据延迟**:原实现每局全部走完才 flush 样本导致 learner 饿死,改为分块 flush 使样本秒级入 buffer;
- **多进程清理**:SIGTERM/超时下保证 worker 先停、再清共享内存,无僵尸进程、无 /dev/shm 泄漏;
- **混合精度**:BF16(A800 原生)。

### 5.5 自动化与无人值守
端到端流水线脚本(setsid 脱离 SSH)串联:数据生成→监督预训练→RL→Elo 评估,并配健康巡检(loss/学习步/GPU 利用率/进程存活)自动发现并修正异常。

![图5.1 跨语言分层系统架构](../logs/fig/fig_arch_system.png)
**图5.1** 跨语言分层:上层 Python+PyTorch(encoding/PVNet/向量化MCTS/selfplay/replay/train/arena/eval),下层 C++17 规则内核(perft 1–5 验证)+Alpha-Beta 引擎,经 pybind11 暴露为 `xqai._xqcore`;唯一跨语言契约=checkpoint 权重文件。

![图5.2 独享 8×A800 分布式拓扑](../logs/fig/fig_topology_8gpu.png)
**图5.2** 8×A800-40GB(NVLink)拓扑:GPU0=learner,GPU1–7=自对弈 actor(每卡 5 worker);ReplayBuffer 驻 /dev/shm(500 万局面),数据/权重落 NVMe;Python MCTS 树操作吃 192 线程 CPU。

---

## 第6章 实验设置

### 6.1 硬件与软件
8× NVIDIA A800-SXM4-40GB(NVLink)、2× AMD EPYC 7K62(192 线程)、1TB 内存;CUDA 12.4 / 驱动 550 / PyTorch 2.6.0+cu124 / g++ 11.4。

### 6.2 数据
监督冷启动数据由 Pikafish(NNUE,约 58 万 NPS)自对弈生成:随机开局 k∈[2,8] 步增多样性,depth 8 搜索,**约 220 万局面 / 1088 分片**(经红黑镜像增广得 ≈445 万训练样本)。结果分布(200 片 / 81.8 万局面随机抽样,见图 7.10):**红胜 21.0% / 和 58.2% / 黑胜 20.9%**——同强度引擎自对弈和棋占比高(>半数),给 value 头提供了大量"和值≈0"的难样本。

### 6.3 棋力评估方法
Arena 配对开局、红黑各半;NetPlayer(net+MCTS)对 Alpha-Beta(xqab,固定 200ms)对弈,胜率经 Wilson 95% CI 与 logistic 公式反推 Elo;每新 checkpoint 自动评估,构建 Elo-训练步曲线。Pikafish 限档可作绝对棋力锚点。

### 6.4 评估协议
消融统一单变量、配对开局;主曲线每 10 分钟一个评估点(20 局);关键结论可加大局数收紧 CI。

> 表6.1 环境与超参;表6.2 评估协议

---

## 第7章 实验结果与分析

### 7.1 正确性验证
见 §3.4:perft(1..5) 逐位一致、规则单测 31/31、编解码视角一致——保证"喂给网络的局面=真实局面"。

### 7.2 监督冷启动(成功)
**256×15 ResNet** 在约 220 万局面(1088 分片,Pikafish depth 8–12)上训 10 个 epoch,平均损失 **5.66→0.167**(对比 128×10 的 0.40,大网拟合更优)。所得模型**对随机走子 16:0(100%)全胜**——证明深度网络通过监督模仿**学会了下真实象棋**(走子合法、不送子、基本战术)。这是本项目"学习型 AI 成立"的核心正面结果。

![图7.1 监督冷启动损失曲线](../logs/fig/fig_pretrain_loss.png)
**图7.1** 监督冷启动损失:10 个 epoch、共 434 个采样点,平均损失自 **5.24 单调降至 0.167**(竖线为 epoch 边界);曲线平滑无震荡,印证大网在 Pikafish 标注数据上拟合充分。

### 7.3 监督训练监控曲线(细粒度指标)
"对随机 100% 胜"在 step 1000 即饱和,作为学习曲线信息量过低。为刻画监督训练的**内部进展**,我们在固定评估子集(16384 局面)上对 21 个 pstep checkpoint 逐个重算三项判别性指标(图7.2):

![图7.2 监督训练监控:策略熵 / top-1 着法命中率 / value 符号准确率](../logs/fig/fig_train_monitor.png)
**图7.2** 监督训练监控(随训练步):**策略熵 2.63→0.24 nats**(策略逐步收敛锐化)、**top-1 着法命中率 27.9%→96.7%**(对 Pikafish 选着的模仿度)、**value 头符号准确率→98.8%**(对局面胜负方向的判断,远高于 0.5 基线;step 5000 处一次回落至 86.9% 后恢复,反映 value 头较 policy 头更晚稳定)。三条曲线共同证明:监督学习真正在"学棋",而非仅记住合法着法。

### 7.4 度量与评测协议的修正(为什么早期结论必须重做)
本工作初期得到过"自对弈 RL 退化、学习型完败 Alpha-Beta"的强负结论。复盘发现这些结论大多是**度量与管线缺陷的伪影**,修正后结论显著改变。三个关键教训:

1. **net-vs-net 相对 Elo 严重通胀**:候选对固定参考网的相对 Elo 可高达 +301,换算到外部引擎锚点≈0;反之候选打不过自己的蒸馏老师却在外部并不弱。**"是否变强"只能以外部锚点为准**(random / 自研 AB / Pikafish)。
2. **评测协议是隐形地雷**:早期引擎对手未接收完整着法历史 → 对"重复局面"判定失明 → 被动型走子被高估,旧 30 局基线虚高约 200 Elo。修复协议(传 `position fen <开局> moves <历史>`)并把样本量提到 64–128 局/点后,基线才可信。
3. **三组管线级 bug(均已修复并通过对抗复审)**:① 自对弈提前退出把约 98% 未完对局强制判和(z=0 污染、无残局样本);② 跨进程写指针误用 threading.Lock(对多进程无效)→ 改 /dev/shm flock;③ 冻结 value 头时 value 损失仍反传进 trunk、BN 统计仍漂移。另有门控"赢者诅咒(中奖测量被当基线棘轮抬高门槛)、评测-晋升竞态、torch 抢满 CPU 饿死自对弈 worker"三连坑。

**修复前所有 RL 相关结论作废**;下文 §7.5–§7.7 全部基于修复版代码 + 外部锚点协议。

### 7.5 ★主结果:监督蒸馏 + DAgger 分布修复使外部棋力 +203 Elo
最终交付模型 **r1**(256×15,v1 监督底座 + DAgger 分布修复蒸馏,`deliverable/r1_champion_256x15.pt`)。128 局/锚点的权威终测(PUCT n_sim=200,修复版评测协议):

| 锚点 | r1 胜率(95%CI) | r1 ΔElo | W-D-L | v1→r1 ΔElo |
|---|---|---|---|---|
| random(自检) | 100% [97.1,100] | (饱和) | 128-0-0 | — |
| 自研 AB-200ms | 27.3% [20.4,35.6] | −170 | 23-24-81 | **+144** |
| 自研 AB-500ms | 28.5% [21.4,36.9] | −160 | 20-33-75 | **+251** |
| Pikafish d1 | 20.3% [14.3,28.1] | −237 | 13-26-89 | **+173** |
| Pikafish d2 | 7.8% [4.3,13.8] | −429 | 7-6-115 | +94 |
| Pikafish d3 | 9.4% [5.4,15.7] | −394 | 8-8-112 | +204 |
| Pikafish d4 | 5.9% [2.9,11.4] | −482 | 5-5-118 | +355 |

**六引擎锚点平均 +203 Elo**(图7.3)。对自研 Alpha-Beta 从约 10% 提到约 28%,对 Pikafish d1 从 8.6% 提到 20.3%;对 random 100% 全胜的自检通过。

![图7.3 v1→r1 各锚点 Elo 提升](../logs/fig/fig_v1_vs_r1.png)
**图7.3(主结果)** r1(DAgger 蒸馏)相对 v1 冷启动底座在全部 6 个引擎锚点上一致提升,平均 +203 Elo。

增益的**唯一来源是分布修复(DAgger)**:把训练局面从"随机走子开局"换成"模型自己的实战分布 + 对引擎败局",用 Pikafish depth-14 标注后从 v1 续训。机理是修复了训练分布与实战分布的系统性错配。三阶段进展见图7.4。

![图7.4 冲刺三阶段外部锚点 Elo 进展](../logs/fig/fig_sprint_ladder.png)
**图7.4** 冲刺三阶段(v1 冷启动 → round-1 DAgger → 128 局终测)对 AB-200ms 与 Pikafish d1 的外部 Elo 单调上升,净提升约 +144 / +173 Elo。

底座质量同样实测(修复协议,外部平均 Elo):**v1(−516) ≫ safe(−693) ≫ v2cold(−815)**——监督模型 v1 是最强底座,故选作 DAgger 续训起点。

### 7.6 自对弈 RL:机制正确、弱底座可爬升、强底座零贡献
管线 bug 修复后,我们用四臂消融在**内部 net-vs-net** 口径下验证自对弈 RL 的机制是否成立(100 局终点 vs 冻结冷启动,n_sim=80):

| 臂 | 相对 Elo(95%CI) | 含义 |
|---|---|---|
| safe(自对弈+rehearsal) | +301 [207,396] | 修复后显著为正 |
| rehNoFrz | +263 [176,351] | — |
| frzV(纯自对弈,修复前为 −301) | +135 [62,208] | 符号翻转,机制成立 |
| suponly(纯蒸馏) | +78 [8,147] | 自对弈增益的 CI 与之不重叠 |

即**修复后自对弈 RL 在内部口径下确有增益**——这推翻了早期"RL 退化"的伪结论。**但换到外部锚点 + 强底座后,增益消失**:以 r1(外部胜率约 0.20)为底座的 B 自对弈循环连续运行 14h / 63832 步 / 79 个候选,**0 晋升**(三配方:lr1e-3 头对头崩溃、lr3e-4 外部单调下跌、冻 value 跑满无一超基线)。

结论是一个有信息量的**条件性负结果**:自对弈改进算子(n_sim=128)在**弱**底座(如 v2cold 外部胜率约 0.10)上尚有爬升空间,故四臂中 safe 的内部 +301 / 早期 +122 成立;一旦底座变强(r1),低模拟自对弈产不出比蒸馏老师更好的训练目标,RL 对最终棋力**零贡献**。**最终棋力增益全部归于监督蒸馏 + DAgger,而非自对弈。** 此外,标签公式工程(A0:WDL 校准锐化 value、policy 温度调尖)经外部锚点严格证伪——浅层 MCTS 均值回传中锐化 value 反而饱和、丧失区分度。

模型对**搜索预算**仍有正向响应(图7.5):小网对弱 AB 的胜率随 n_sim 60→600 翻倍(0.156→0.312),说明瓶颈在规模/算力而非算法正确性。

![图7.5 n_sim 消融:搜索预算的正向响应](../logs/fig/fig_nsim_ablation.png)
**图7.5** 更多模拟→更强:n_sim 60→600 对弱 AB 胜率翻倍,佐证瓶颈在算力规模而非算法正确性。

### 7.7 学习型 vs Alpha-Beta:差距收窄约 200 Elo,但未出现 crossover
r1 对自研 Alpha-Beta 的终测胜率:200ms 档 **27.3%**、500ms 档 **28.5%**(各 128 局),相比 v1 底座的约 10% 大幅提升,把修正后的差距收窄约 200 Elo,但**仍显著低于 0.5,未出现学习型反超 Alpha-Beta 的 crossover**(图7.6)。值得注意:r1 对 500ms 档(28.5%)略高于 200ms 档(27.3%)——给对手更多时间未拉大差距,反映中等深度上双方接近;而 Pikafish 各档(d1 20.3% → d4 5.9%)显示对更强引擎差距迅速拉大。

![图7.6 r1 对各外部锚点终测棋力](../logs/fig/fig_anchor_final.png)
**图7.6** r1 对 7 个外部锚点的终测(128 局/点):对 random 100% 自检通过,对自研 AB 约 28%,对 Pikafish 随档位升高胜率下降。

棋力天花板的本质:本工作每步约 200 次模拟,而 Alpha-Beta 每步搜索数十万节点,Elo 指数尺度下同预算极不对称;蒸馏上限 = 老师在标注深度判断的一次前向压缩,丢失了决定胜负的战术尾部。Alpha-Beta 基线自身搜索效率良好(见 §7.8)。

### 7.8 效率与系统指标
**自对弈吞吐**(单卡 PVNet(128,10),n_sim=32)实测(图7.7):

| 并行对局 | ms/步 | moves/s | 单卡≈局/小时 |
|---|---|---|---|
| 64 | 465 | 138 | 4952 |
| 256 | 1578 | 162 | 5842 |
| 512 | 3142 | 163 | 5867 |

![图7.7 自对弈吞吐扩展](../logs/fig/fig_throughput.png)
**图7.7** moves/s 随并行数饱和于 ~163,瓶颈在 Python MCTS 树操作(CPU 端);故采用**多 worker 进程横向扩展吃满 192 核 + 集群级聚合利用率**补回单任务效率(对应图5.2 拓扑)。注:正因单卡推理非瓶颈、GPU 利用率不饱和,系统才以"每卡多 worker"提高聚合 GPU 占用,而非追求单进程满载——这也是不上 C++ LibTorch 推理服务的依据。

**Alpha-Beta 基线搜索效率**(12 个多样局面/时限,xqab 内置计数器实测):

| AB 时限 | 平均完成深度 | 平均节点数 | NPS | 置换表命中率 | β-剪枝率 | 首着剪枝率 |
|---|---|---|---|---|---|---|
| 50ms | 4.3 | 9.4万 | ~1.84M | 4.2% | 79.3% | 76.6% |
| 200ms | 5.2 | 33.4万 | ~1.66M | 8.4% | 85.7% | 82.3% |
| 500ms | 5.8 | 77.3万 | ~1.54M | 9.6% | 87.1% | 79.9% |

分析:**首着剪枝率高达 77–82%**——表示 MVV-LVA + 置换表着法排序质量优良(绝大多数 β 截断发生在第一个搜索着法上,接近理论最优的 αβ 行为);置换表命中率随时限上升(更深搜索复用更多);NPS ~1.5–1.8M(单线程)。

### 7.9 训练监控小结
本章已用真实数据补齐全部监控维度:监督 loss(图7.1)、policy 熵 / top-1 命中率 / value 符号准确率(图7.2)、棋力提升主结果与三阶段进展(图7.3、图7.4)、n_sim 消融(图7.5)、终测各锚点(图7.6)、自对弈吞吐(图7.7)、数据结果分布与自对弈平局率(图7.10)、对局长度分布(图7.11)。监督训练全程 policy 熵单调下降而无骤崩、对局长度均值合理(非速败/速和),排除了静默坍缩。

![图7.10 监督数据结果分布](../logs/fig/fig_result_dist.png)
**图7.10** Pikafish depth-8 自对弈数据结果分布(81.8 万局面):红胜 21.0% / 和 58.2% / 黑胜 20.9%。

![图7.11 冷启动模型自对弈对局长度分布](../logs/fig/fig_game_length.png)
**图7.11** 冷启动模型(256×15,PUCT n_sim=40)自对弈 40 局的半回合数分布:**均值 141.1、中位数 120**、范围 [41, 300],自对弈平局率 15.0%(达 240 半回合自然限着计和)。对局长度合理(非速败/速和),进一步佐证模型走子有章法。

---

## 第8章 总结与展望

### 8.1 结论
回答研究问题——**在 8×A800、约一周预算下,"监督冷启动 + 自对弈"两阶段管线能否超越同等预算的 Alpha-Beta?** 本文给出**否定、但差距大幅收窄且方法论上有信息量**的答案:

1. **学习型 AI 成立**:深度网络(256×15)经监督模仿学会下真实象棋(100% 胜随机、top-1 模仿 96.7%、value 符号准确率 98.8%),系统全链路(C++ 规则内核 perft 全对、向量化批量 MCTS、Gumbel、8 卡分布式)正确可运行。
2. **棋力大幅提升但未反超**:最终模型 r1 对 6 个引擎锚点**平均 +203 Elo**(对自研 AB 约 28%、对 Pikafish d1 约 20%),把差距收窄约 200 Elo,但所有预算下仍弱于 Alpha-Beta,未出现 crossover。
3. **增益来源的诚实归因**:全部棋力增益来自**监督蒸馏 + DAgger 分布修复**;自对弈 RL 在管线 bug 修复后机制正确、在弱底座上可爬升(内部四臂 frzV 由 −301 翻正到 +135),但在已较强的 r1 底座上**零贡献**(14h / 79 候选 / 0 晋升);标签公式工程(WDL 锐化)经外部锚点证伪。
4. **方法论副产品(比棋力数字更可复用)**:net-vs-net Elo 不可信(+301 内部 ≈ 0 外部);评测协议不修则基线虚高约 200 Elo;门控三连坑(赢者诅咒 / 评测-晋升竞态 / torch 抢 CPU 饿死 worker);低算力下低模拟自对弈的"改进信号"在强底座上易被淹没。

**贡献**:本文不是又一个"调通 Alpha-Beta",而是在严格有限算力下系统、诚实地刻画了学习型方法相对传统搜索的能力边界,定位了真正起作用的杠杆(**分布修复**)与失效的方向(**强底座低模拟自对弈、标签工程**),并沉淀了一套以外部锚点为准的评测方法论。这对"有限资源下该把算力投向哪里"是有价值的实证结论。

### 8.2 局限
长将/长捉规则简化为重复判和;训练步数受一周预算限制;r1 训练损失平台约 2.52,256×15 可能已触容量上限;自对弈在强底座上未能贡献增益;Gumbel 逐局串行未充分向量化;蒸馏上限受教师标注深度限制;未上 TensorRT。

### 8.3 展望
最值得试的是**容量杠杆**(384×20 同蒸馏数据重训,验证是否为容量瓶颈,需多卡约 3h);其次:完整长将规则、更长训练、提高自对弈 n_sim(256+)以增强改进算子、多卡 DDP learner、2086 紧凑动作编码、SE-block/注意力、NNUE 评估对照、Transformer/searchless 路线、扩展到掼蛋/麻将等不完全信息博弈。

---

## 附录

### A. 关键超参
网络 128×10(主)/256×15(消融);MCTS n_sim 自对弈 32~48、评估 100~200;Dirichlet(0.2,0.25);虚拟损失 3;lr 监督 1e-3、RL 2e-2→1e-4;L2 1e-4;batch 监督 1024 / RL 2048;BF16;ReplayBuffer 200 万。

### B. 复现说明
环境:`uv venv` + 清华镜像装 torch==2.6.0(cu124);`uv pip install -e .` 编译 _xqcore;数据 `scripts/gen_pikafish_data.py`;训练 `scripts/run_pipeline.sh`;评估 `scripts/eval_loop.py`。

### C. 数据来源与参考论文 / 开源声明(任务书硬性要求)
**论文**:AlphaGo(Silver 2016)、AlphaGo Zero(2017)、AlphaZero(2018)、Gumbel MuZero/AlphaZero(Danihelka ICLR 2022)、MuZero(2020)、EfficientZero V2(2024)、ReZero(2024)、Search-contempt(2025)、Student of Games(2023)、AlphaZe**(2023)、DeepStack/Libratus/Pluribus/DeepCFR/Suphx、KataGo(Wu 2020)、Warm-Start AlphaZero(2020)、SENet(2018)、Searchless Chess/Chessformer/Maia(2024–2026)。
**开源**:Pikafish(强对手/数据生成/锚定)、ChineseChess-AlphaZero(2086 编码参考)、OpenSpiel / LightZero / TurboZero / MCTX(MCTS/Gumbel 参考)、PyTorch / pybind11。
**数据**:监督冷启动数据由本项目用 Pikafish 自对弈生成(非外部下载)。
